version: "3.7"
services:
  app:
    build: .
    restart: always
    ports:
      - 8080:8080
    environment:
      - REST_REDIS_ADDR=redis:6379
      - REST_POSTGRES_HOST=postgres
    depends_on:
      - postgres
      - redis
    networks:
      - internal
  redis:
    image: redis:5.0.5-alpine
    ports:
      - 6379:6379
    networks:
      - internal
  postgres:
    image: postgres:11.3-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres
    ports:
      - 5432:5432
    networks:
      - internal

networks:
  internal:
    driver: bridge
